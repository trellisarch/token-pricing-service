image: 
  tag: {{ .StateValues.ci.tag }}

autoscaling:
  enabled: false

metrics:
  enabled: false

ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: nginx
  hosts:
    - host: token-price-dev.rdx-works-main.extratools.works
      paths:
        - path: /
          pathType: Prefix

envs:
  DB_HOST: "airflow-dev.cluster-cd2yqpprgk5z.eu-west-2.rds.amazonaws.com"
  DB_NAME: "dags"
  DB_USER: "postgres"

secrets:
  - name: rds-password
    variable: "DB_PASSWORD"
    path: "rdx-works-main/eks/token-price/dev/postgres-creds"
    secretProperty: "postgresql-password"
    secretName: "database-password"
