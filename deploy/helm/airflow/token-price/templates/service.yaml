---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "token-price.fullname" . }}
  labels:
    {{- include "token-price.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "token-price.selectorLabels" . | nindent 4 }}
{{- if .Values.metrics.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "token-price.fullname" . }}-metrics
  labels:
    {{- include "token-price.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.metrics.port }}
    name: metrics
    targetPort: {{ .Values.metrics.port }}
  selector:
    {{- include "token-price.selectorLabels" . | nindent 4 }}
{{- end }}
